[package]
name = "lau-a2ui"
version = "0.1.0"
edition = "2021"
description = "Agent-to-UI rendering protocol — rendering-system-agnostic state representation"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rmp-serde = "1"
